apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: web-bansach
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: hoangdev311/web-bansach-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: web-bansach
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
